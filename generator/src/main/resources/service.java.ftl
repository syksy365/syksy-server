package ${packagePath}.service;

import ${packagePath}.domain.${domainName};
import com.baomidou.mybatisplus.extension.service.IService;

/**
 * @author ${author}
 */
public interface ${serviceName} extends IService<${domainName}> {

}
