package ${packagePath}.service.impl;

import ${packagePath}.domain.${domainName};
import ${packagePath}.mapper.${mapperName};
import ${packagePath}.service.${serviceName};

import com.baomidou.mybatisplus.extension.service.impl.ServiceImpl;
import org.springframework.stereotype.Service;

/**
 * @author ${author}
 */
@Service
public class ${serviceName}Impl extends ServiceImpl<${mapperName}, ${domainName}> implements ${serviceName}{

}
